name: CD - Production (manual)

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: 'Git SHA or release tag to deploy'
        required: true
        type: string
      confirm:
        description: 'Type DEPLOY-PRODUCTION to confirm a production deployment'
        required: true
        type: string

permissions:
  contents: read

jobs:
  preflight:
    if: ${{ inputs.confirm == 'DEPLOY-PRODUCTION' }}
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.image_tag }}
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - run: dotnet restore LogicFit.sln
      - run: dotnet build LogicFit.sln -c Release --no-restore
      - run: dotnet test LogicFit.sln -c Release --no-build --verbosity normal
      - run: dotnet tool install --global dotnet-ef --version 8.*
      - run: dotnet ef migrations script --idempotent --no-build --project LogicFit.Infrastructure --startup-project LogicFit.API --output /tmp/logicfit-migrations.sql

  deploy:
    if: ${{ inputs.confirm == 'DEPLOY-PRODUCTION' }}
    needs: preflight
    runs-on: windows-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.image_tag }}

      - name: Require unified WebDeploy profile
        shell: pwsh
        env:
          UNIFIED_PROFILE_B64: ${{ secrets.RUNASP_UNIFIED_PUBLISH_SETTINGS_B64 }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:UNIFIED_PROFILE_B64)) {
            throw "RUNASP_UNIFIED_PUBLISH_SETTINGS_B64 is required."
          }

      - name: Build publish outputs
        shell: pwsh
        run: |
          dotnet publish LogicFit.API/LogicFit.API.csproj -c Release -o "$env:RUNNER_TEMP/unified-publish" /p:UseAppHost=false

      - name: Decode protected publish profiles
        shell: pwsh
        env:
          UNIFIED_PROFILE_B64: ${{ secrets.RUNASP_UNIFIED_PUBLISH_SETTINGS_B64 }}
        run: |
          [IO.File]::WriteAllBytes("$env:RUNNER_TEMP/unified.publishSettings", [Convert]::FromBase64String($env:UNIFIED_PROFILE_B64))

      - name: Deploy unified LogicFit API with WebDeploy
        shell: pwsh
        run: .\Scripts\deploy-webdeploy.ps1 -PublishSettingsPath "$env:RUNNER_TEMP/unified.publishSettings" -ContentPath "$env:RUNNER_TEMP/unified-publish"

      - name: Verify production health
        shell: pwsh
        env:
          UNIFIED_HEALTH_URL: ${{ secrets.RUNASP_UNIFIED_HEALTHCHECK_URL }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:UNIFIED_HEALTH_URL)) {
            throw "RUNASP_UNIFIED_HEALTHCHECK_URL is required."
          }
          $response = Invoke-WebRequest -Uri $env:UNIFIED_HEALTH_URL -UseBasicParsing -TimeoutSec 30
          if ($response.StatusCode -ne 200) { throw "Health check failed for $env:UNIFIED_HEALTH_URL" }
